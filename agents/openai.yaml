interface:
  display_name: "Work"
  short_description: "Lead evidence-driven work with bounded agents"
  default_prompt: "Use $work to define the exact goal, choose the smallest useful team, execute with complete briefs, and verify concrete evidence; use $work-ideate to challenge assumptions and shape a testable direction, or choose $work-plan and $work-audit for read-only modes."
policy:
  allow_implicit_invocation: false
