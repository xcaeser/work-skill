interface:
  display_name: "Work"
  short_description: "Plan, audit, clean, and run with focused agents"
  default_prompt: "Use $work to plan, audit, clean, or run work with focused agents, or use $work help to see commands."
policy:
  allow_implicit_invocation: false
