interface:
  display_name: "Work"
  short_description: "Plan, execute, audit, and track agent work"
  default_prompt: "Use $work help to choose a workflow, or $work boss to execute a task through bounded agents."
policy:
  allow_implicit_invocation: false
