interface:
  display_name: "Work"
  short_description: "Lead free-form work with bounded agents"
  default_prompt: "Use $work for ordinary or advanced execution, or choose a dedicated mode such as $work-plan or $work-audit."
policy:
  allow_implicit_invocation: false
