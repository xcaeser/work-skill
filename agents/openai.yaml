interface:
  display_name: "Work"
  short_description: "Delegate bounded work and prove the integrated result"
  default_prompt: "Use $work to define one observable goal, launch the smallest useful executor team with complete task packets and disjoint ownership, integrate their work, and prove the result with concrete validation."
policy:
  allow_implicit_invocation: false
