interface:
  display_name: "Work"
  short_description: "Create, plan, run, clean, and review projects"
  default_prompt: "Use $work help to choose an action, or $work run to complete a task with focused agents."
policy:
  allow_implicit_invocation: false
