interface:
  display_name: "Work / Plan"
  short_description: "Turn a task into a detailed Luna Max execution plan"
  default_prompt: "Use $work-plan to create a detailed read-only execution plan."
policy:
  allow_implicit_invocation: false
