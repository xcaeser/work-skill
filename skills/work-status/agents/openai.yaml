interface:
  display_name: "Work / Status"
  short_description: "Show the current Work goal and agent state"
  default_prompt: "Use $work-status to show the current Work goal, agents, and next action."
policy:
  allow_implicit_invocation: false
