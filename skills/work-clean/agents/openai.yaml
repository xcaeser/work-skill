interface:
  display_name: "Work / Clean"
  short_description: "Read-only Sol xhigh cleanup plans for simpler code"
  default_prompt: "Use $work-clean with Sol xhigh to find code smells and produce a detailed Luna Max cleanup plan."
policy:
  allow_implicit_invocation: false
